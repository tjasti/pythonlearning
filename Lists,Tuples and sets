
subjects=['maths','science','social']
print(subjects)
subjects.append('english')
print("after appending")
print(subjects)
#clear function, it will clear the items from the list but it will keep the structure
#subjects.clear()
print(subjects)
#Adding items back to the list
#copy method
subjects1=subjects.copy()
print(subjects1)
